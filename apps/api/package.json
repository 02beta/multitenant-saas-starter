{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm run dev:kill && uv run fastapi dev src/api/main.py --host 0.0.0.0 --port 8000",
    "dev:kill": "pkill -f 'fastapi|uvicorn' || true",
    "start": "uv run fastapi run src/api/main.py --host 0.0.0.0 --port 8000",
    "build": "echo 'No build step required for FastAPI'",
    "clean": "pkill -f 'fastapi|uvicorn' || true"
  }
}
