export type { Fields, LogLevel } from "./shared";

// Import one of the runtime-specific implementations:
//   import { logger, withAxiom } from "@workspace/logger/next";
//   import { logger } from "@workspace/logger/node";
